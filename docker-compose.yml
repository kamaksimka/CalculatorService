version: '3.8'

services:
  calculator-api:
    build:
      context: ./src
      dockerfile: ./CalculatorService.API/Dockerfile
    ports:
      - "10500:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - ./logs:/app/logs